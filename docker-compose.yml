---
services:
  app:
    build:
      context: .
      args:
        - FREELENS_VERSION=1.2.0
    ports:
      - 5900:5900
      - 8080:8080
    volumes:
      - ./app:/app
      - ./root:/root
      - ./kubeconfig:/root/.kube/config
